env_id: email-triage-env
version: "1.0.0"

tasks:
  - id: easy
    grader:
      type: llm
      prompt_template: |
        You are a strict grader.
        Return ONLY a number between 0.1 and 0.9.
        Do NOT return any text.
        Only output the number.

  - id: medium
    grader:
      type: llm
      prompt_template: |
        You are a strict grader.
        Return ONLY a number between 0.1 and 0.9.
        Do NOT return any text.
        Only output the number.

  - id: hard
    grader:
      type: llm
      prompt_template: |
        You are a strict grader.
        Return ONLY a number between 0.1 and 0.9.
        Do NOT return any text.
        Only output the number.